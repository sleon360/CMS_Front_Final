package com.appcms.entity;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertNotEquals;
import static org.mockito.Mockito.mock;
import static org.mockito.Mockito.when;

import org.junit.Before;
import org.junit.Test;

public class ViewEntityTest {
/*
	private ViewEntity viewEntity;
	
	@Before
	public void setUp() throws Exception {
		viewEntity=new ViewEntity();
		viewEntity.setId_view(55);
		viewEntity.setIdviewfk("COD66");
		viewEntity.setStatus("ACTIVO");
		viewEntity.setName("VISTA_HOME");
		viewEntity.setContent("<!DOCTYPE html>\r\n" + 
				"<html>\r\n" + 
				"<head>\r\n" + 
				"<meta charset=\"UTF-8\">\r\n" + 
				"<title>Title of the document</title>\r\n" + 
				"</head>\r\n" + 
				"\r\n" + 
				"<body>\r\n" + 
				"Content of the document......\r\n" + 
				"</body>\r\n" + 
				"\r\n" + 
				"</html>");
	}

	@Test
	public final void testGetId_view() throws Exception {
		ViewEntity esperado = mock(ViewEntity.class);
		ViewEntity noesperado = mock(ViewEntity.class);
		when(esperado.getId_view()).thenReturn(55);
		when(noesperado.getId_view()).thenReturn(300);
		assertEquals(viewEntity.getId_view(), esperado.getId_view());
		assertNotEquals(noesperado.getId_view(), viewEntity.getId_view());
	}

	@Test
	public final void testGetName() throws Exception {
		ViewEntity esperado = mock(ViewEntity.class);
		ViewEntity noesperado = mock(ViewEntity.class);
		when(esperado.getName()).thenReturn("VISTA_HOME");
		when(noesperado.getName()).thenReturn("NO_VISTA_HOME");
		assertEquals(viewEntity.getName(), esperado.getName());
		assertNotEquals(noesperado.getName(), viewEntity.getName());
	}

	@Test
	public final void testGetContent() throws Exception {
		ViewEntity esperado = mock(ViewEntity.class);
		ViewEntity noesperado = mock(ViewEntity.class);
		when(esperado.getContent()).thenReturn("<!DOCTYPE html>\r\n" + 
				"<html>\r\n" + 
				"<head>\r\n" + 
				"<meta charset=\"UTF-8\">\r\n" + 
				"<title>Title of the document</title>\r\n" + 
				"</head>\r\n" + 
				"\r\n" + 
				"<body>\r\n" + 
				"Content of the document......\r\n" + 
				"</body>\r\n" + 
				"\r\n" + 
				"</html>");
		when(noesperado.getContent()).thenReturn("NO_VISTA_HOME_HTML");
		assertEquals(viewEntity.getContent(), esperado.getContent());
		assertNotEquals(noesperado.getContent(), viewEntity.getContent());
	}

	@Test
	public final void testGetStatus() throws Exception {
		ViewEntity esperado = mock(ViewEntity.class);
		ViewEntity noesperado = mock(ViewEntity.class);
		when(esperado.getStatus()).thenReturn("ACTIVO");
		when(noesperado.getStatus()).thenReturn("NO_ACTIVO");
		assertEquals(viewEntity.getStatus(), esperado.getStatus());
		assertNotEquals(noesperado.getStatus(), viewEntity.getStatus());
	}

	@Test
	public final void testGetIdviewfk() throws Exception {
		ViewEntity esperado = mock(ViewEntity.class);
		ViewEntity noesperado = mock(ViewEntity.class);
		when(esperado.getIdviewfk()).thenReturn("COD66");
		when(noesperado.getIdviewfk()).thenReturn("COD61");
		assertEquals(viewEntity.getIdviewfk(), esperado.getIdviewfk());
		assertNotEquals(noesperado.getIdviewfk(), viewEntity.getIdviewfk());
	}
*/
}
